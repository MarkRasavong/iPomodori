{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "install-client": "cd client && npm install",
    "client": "cd .. && cd client && npm start",
    "dev": "concurrently --names 'server,client' --prefix-colors 'red,blue' \"nodemon server.js\" \"npm run client\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "json-server": "^0.16.3",
    "mongoose": "^5.13.7",
    "nanoid": "^3.1.25",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "concurrently": "^6.2.1",
    "nodemon": "^2.0.12"
  }
}
